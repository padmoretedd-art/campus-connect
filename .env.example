# Environment variables will be added here as each phase introduces them.
# Never commit a real .env file — it is already in .gitignore.

# Database
DATABASE_URL="postgresql://campus_connect:campus_connect_dev_password@localhost:5432/campus_connect_dev"
